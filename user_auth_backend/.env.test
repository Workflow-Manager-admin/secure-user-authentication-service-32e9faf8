# Test Environment Configuration
NODE_ENV=test
PORT=3001
HOST=0.0.0.0

# Test Database Configuration (using in-memory MongoDB or mock)
MONGODB_URI=mongodb://localhost:27017/auth-test-db
MONGODB_TEST_URI=mongodb://localhost:27017/auth-test-db

# JWT Configuration for testing
JWT_SECRET=test-jwt-secret-key-for-testing-12345
JWT_EXPIRES_IN=1h

# Password Hashing (lower rounds for faster testing)
BCRYPT_ROUNDS=1
